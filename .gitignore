# Local tooling — preview launch config, agent scratch dirs.
.claude/

# Editor / OS junk.
.DS_Store
Thumbs.db
*~
*.swp
.vscode/
.idea/

# Transient commit-message scratch files used by the dev tooling.
.commit-msg.tmp

# npm artefacts (docx generation, one-off scripts).
node_modules/
package.json
package-lock.json
make-form.js
